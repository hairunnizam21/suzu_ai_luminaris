package com.suzuai.luminaris.shared.theme

import androidx.compose.ui.graphics.Color

/**
 * The single Luminaris palette. Black canvas, cyan accents, mono-feel.
 * Both APKs share these so they look like one product.
 */
object LuminarisColors {
    val Bg = Color(0xFF050608)
    val Surface = Color(0xFF0B0E12)
    val SurfaceAlt = Color(0xFF121620)
    val Outline = Color(0xFF1F2733)
    val Divider = Color(0xFF1A2230)

    val Text = Color(0xFFE6EEF7)
    val TextDim = Color(0xFF9AA7B7)
    val Muted = Color(0xFF5B6675)

    val AccentCyan = Color(0xFF22D3EE)
    val AccentCyanDim = Color(0xFF0E7490)
    val AccentGreen = Color(0xFF34D399)
    val AccentRed = Color(0xFFF87171)
    val AccentAmber = Color(0xFFFBBF24)

    val UserBubble = Color(0xFF13202D)
    val ToolBubble = Color(0xFF0F1A24)
}
